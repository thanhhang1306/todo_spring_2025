name: todo_spring_2025
description: "DART Sidious's TODO"
publish_to: 'none'

version: 2.1.0+20252

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.1
  sign_in_button: ^3.2.0
  firebase_auth: ^5.5.1
  google_sign_in: ^6.2.2
  cloud_firestore: ^5.6.5
  google_fonts: ^6.2.1
  flutter_local_notifications: ^19.0.0
  flutter_launcher_icons: ^0.13.1
  app_settings: ^5.1.1
  timezone: ^0.10.0
  table_calendar: ^3.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_launcher_icons:
  android: true
  image_path: "icon/icon.png" # Caminho para o seu ícone principal

flutter:
  uses-material-design: true

  assets:
    - fonts/

  fonts:
    - family: PressStart2P
      fonts:
        - asset: fonts/PressStart2P-Regular.ttf